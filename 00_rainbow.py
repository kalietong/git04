
def print_colorful_text():
    string.count('rainbow')
    count=0
    while len > count:
        for each chr() in string:
            count=count+1
            if count = 1
            text is blue
    for style in range(8):
        text.configure(font=(Times New Roman, 12, "bold"))
        for foreground_color in range (30,38):
            sl = ''
            for background_color in range (40,48):
                sl += '\x1b[%sm %s \x1b[0m' % (format, rainbow)
            print(sl)
        print('\n')

print_colorful_text()


